# Allow build scripts for packages that need native compilation
enable-pre-post-scripts=true

# Explicitly allow build scripts for specific packages
# These packages use native binaries and need to run their build scripts
# @parcel/watcher - File watching for Nuxt/Vite
# esbuild - JavaScript bundler
# sharp - Image processing
# workerd - Cloudflare Workers runtime
